using NetflixStream.Application.Interfaces;
using NetflixStream.Domain.Entities;
using NetflixStream.Domain.UserIdentity;
using NetflixStream.Infrastructure.Repositories;
using NetflixStream.Persistence;
using NUnit.Framework;
using System;

namespace NetflixStream.UnitTests.Repositories
{
    [TestFixture]
    public class TestVideosRespositores
    {
        [Test]
        public void Sum_ShouldReturnCorrectResult_WhenCalledWithTwoIntegers()
        {
            // Arrange
            //var country = new VideosRespositores();

           // // Act
           // int expected =10;
           // int actual = country.Sum(5, 5); // Ensure `Sum` is a public method in `Country`

           // // Assert
           //// Assert.AreEqual(expected, actual, "The sum should be 10");

           // Assert.That(expected, Is.EqualTo(actual));
        }
    }
}
